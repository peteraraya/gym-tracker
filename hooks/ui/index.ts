/**
 * hooks/ui — Hooks de estado de UI reutilizables
 *
 * Estos hooks encapsulan lógica de interfaz de usuario que no pertenece
 * a ningún dominio de negocio específico.
 */

export { useAccessibility } from "./useAccessibility";
export { useFilteredData } from "./useFilteredData";
export { useNumberInput } from "./useNumberInput";
export { usePagination } from "./usePagination";
